# @package _global_
defaults:
  - override /data: geo_pmw_near89_common10_era5
  - override /model: direct_unet

trainer:
  max_epochs: 10_000
  checkpoint:
    monitor: val/rmse_k
    mode: min
    save_top_k: 2
    save_last: true
logging:
  wandb:
    name: geo-pmw-near89-common10-era5-direct-unet
